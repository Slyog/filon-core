// Legacy QA suite for the old session / RestoreToast system.
// This file is intentionally skipped after the simpleCanvasSession rewrite.
// TODO(FILON Step 20.x): Replace with new QA tests for EmptyWorkspace + simple session restore.

describe.skip("qa-session-restore (legacy)", () => {
  it("is temporarily disabled after the simple session rewrite", () => {
    expect(true).toBe(true);
  });
});
